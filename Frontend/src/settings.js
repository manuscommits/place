const frameRate = 1;

const size = 10;
const xMaxPlace = 150;
const yMaxPlace = 90;

const canvasWidth = size * xMaxPlace;
const canvasHeight = size * yMaxPlace;

const lightLine = 100;
const mediumLine = 50;
const strongLine = 0;

export {
    frameRate,
    size,
    xMaxPlace,
    yMaxPlace,
    canvasWidth,
    canvasHeight,
    lightLine,
    mediumLine,
    strongLine,
};

